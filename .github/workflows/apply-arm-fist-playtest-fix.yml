name: Apply arm and fist playtest fix (manual only)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Use the review branch helper workflow for this one-shot fix."
