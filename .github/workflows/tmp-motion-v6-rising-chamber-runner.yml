name: Temporary V6 rising chamber branch runner

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/tmp-motion-v6-rising-chamber-runner.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: motion-foundry-v6-reference-driven
          fetch-depth: 0
      - name: Apply targeted V6 rising chamber lift
        run: |
          set -euo pipefail
          python3 scripts/patch-v6-rising-chamber.py
          python3 -m py_compile tools/blender/build-fight-motion-foundry-v2-kicks.py
          git diff --check
      - name: Commit targeted V6 rising chamber lift
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tools/blender/build-fight-motion-foundry-v2-kicks.py
          if git diff --cached --quiet; then
            echo "No V6 changes to commit."
            exit 0
          fi
          git commit -m "Raise measured rising kick chamber"
          git push origin HEAD:motion-foundry-v6-reference-driven
