name: Reactable Trace Runner

on:
  push:
    branches: [reactable-enemy-attacks-v1]
    paths:
      - .github/workflows/reactable-trace-runner.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: reactable-enemy-attacks-v1
      - name: Patch audit trace
        run: python3 scripts/reactable-trace-patch.py
      - name: Commit trace
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add scripts/capture-tps-visual-audit.mjs
          git commit -m "Trace reactive STEP frame state in TPS audit"
          git push origin HEAD:reactable-enemy-attacks-v1
