name: POLY FIGHTER AXION WebGL audit

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - app/**
      - src/game/**
      - public/models/**
      - package-lock.json
      - scripts/capture-axion-playable-audit.mjs
      - .github/workflows/axion-fighter-audit.yml
  pull_request:
    branches:
      - main
    paths:
      - app/**
      - src/game/**
      - public/models/**
      - package-lock.json
      - scripts/capture-axion-playable-audit.mjs
      - .github/workflows/axion-fighter-audit.yml

permissions:
  contents: read

concurrency:
  group: axion-webgl-audit-${{ github.ref }}
  cancel-in-progress: true

jobs:
  capture:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Build production preview
        run: npm run build
      - name: Start production preview
        run: |
          PORT=3000 npm start > /tmp/poly-fighter-axion-server.log 2>&1 &
          echo $! > /tmp/poly-fighter-axion-server.pid
          for attempt in $(seq 1 45); do
            if curl --fail --silent http://127.0.0.1:3000/ > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/poly-fighter-axion-server.log
          exit 1
      - name: Capture AXION playable fights
        run: |
          mkdir -p artifacts/axion-webgl-audit
          DRIVER="$(command -v chromedriver || true)"
          if [ -z "$DRIVER" ] && [ -n "${CHROMEWEBDRIVER:-}" ] && [ -x "$CHROMEWEBDRIVER/chromedriver" ]; then
            DRIVER="$CHROMEWEBDRIVER/chromedriver"
          fi
          if [ -z "$DRIVER" ]; then
            echo "ChromeDriver is not available"
            exit 1
          fi
          WEBDRIVER_BIN="$DRIVER" AUDIT_URL="http://127.0.0.1:3000/" TPS_AUDIT_DIR="artifacts/axion-webgl-audit" node scripts/capture-axion-playable-audit.mjs
          for image in \
            axion-playable-loadout-iphone \
            axion-playable-p1-iphone \
            axion-playable-p2-iphone; do
            test -s "artifacts/axion-webgl-audit/${image}.png"
            file "artifacts/axion-webgl-audit/${image}.png" | grep -q "PNG image data"
          done
          test -s artifacts/axion-webgl-audit/axion-playable.json
      - name: Preserve server log
        if: always()
        run: |
          mkdir -p artifacts/axion-webgl-audit
          if [ -f /tmp/poly-fighter-axion-server.log ]; then
            cp /tmp/poly-fighter-axion-server.log artifacts/axion-webgl-audit/server.log
          fi
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: poly-fighter-axion-webgl-audit
          path: artifacts/axion-webgl-audit
          if-no-files-found: error
          retention-days: 7
