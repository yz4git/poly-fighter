from pathlib import Path


def replace_once(path: str, old: str, new: str, label: str) -> None:
    file = Path(path)
    text = file.read_text()
    if old not in text:
        raise SystemExit(f"missing patch target: {label} in {path}")
    text = text.replace(old, new, 1)
    file.write_text(text)


# ---------------------------------------------------------------------------
# motion-profile.ts: authored link window + continuation branching
# ---------------------------------------------------------------------------
replace_once(
    "src/game/motion-profile.ts",
    'export type TpsComboRoute = "CLOSE_A" | "CLOSE_B" | "FAR" | "FLANK" | "PERFECT";\n',
    '''export type TpsComboRoute = "CLOSE_A" | "CLOSE_B" | "FAR" | "FLANK" | "PERFECT";\n\nexport type TpsComboLinkWindow = {\n  queueStart: number;\n  linkStart: number;\n  linkEnd: number;\n};\n''',
    "combo link type",
)

old_choose = '''export function chooseTpsComboRoute(input: {\n  distance: number;\n  flank: boolean;\n  perfect: boolean;\n  variationSeed: number;\n}): TpsComboRoute {\n  if (input.perfect) return "PERFECT";\n  if (input.flank) return "FLANK";\n  if (input.distance > 1.58) return "FAR";\n  return input.variationSeed % 2 === 0 ? "CLOSE_A" : "CLOSE_B";\n}\n'''
new_choose = old_choose + '''\n/**\n * A combo link is deliberately narrower than the whole recovery period.\n * ATTACK may be buffered shortly before recovery, but the actual branch only\n * fires after a small visible settle beat and before the authored cancel window\n * closes. Gameplay therefore stays responsive without visually snapping from\n * impact directly into the next startup.\n */\nexport function tpsComboLinkWindow(move: MoveDefinition): TpsComboLinkWindow {\n  const recoveryStart = move.startup + move.active;\n  const recoveryTicks = Math.max(1, move.recovery);\n  const usableRecovery = Math.max(1, recoveryTicks - 2);\n  const authoredWindow = Math.max(1, Math.min(usableRecovery, move.cancelWindow));\n  const settleTicks = Math.max(1, Math.min(usableRecovery, Math.round(recoveryTicks * 0.22)));\n  const linkStart = recoveryStart + settleTicks;\n  const linkEnd = Math.min(recoveryStart + recoveryTicks - 1, linkStart + authoredWindow - 1);\n  return {\n    queueStart: Math.max(move.startup, recoveryStart - 2),\n    linkStart,\n    linkEnd,\n  };\n}\n\n/**\n * Direction held with the buffered ATTACK chooses the continuation at the link\n * point. Earned PERFECT/FLANK routes stay locked; ordinary strings may change\n * between pressure and angle routes, while knockback naturally promotes a FAR\n * continuation. No extra input button is required.\n */\nexport function chooseTpsComboContinuationRoute(input: {\n  currentRoute: TpsComboRoute;\n  distance: number;\n  forward: boolean;\n  back: boolean;\n  side: boolean;\n}): TpsComboRoute {\n  if (input.currentRoute === "PERFECT" || input.currentRoute === "FLANK") return input.currentRoute;\n  if (input.distance > 1.72) return "FAR";\n  if (input.side || input.back) return "CLOSE_B";\n  if (input.forward) return "CLOSE_A";\n  if (input.currentRoute === "FAR") return "CLOSE_A";\n  return input.currentRoute;\n}\n'''
replace_once("src/game/motion-profile.ts", old_choose, new_choose, "combo link helpers")

# ---------------------------------------------------------------------------
# tps-game.ts: buffer branch intent and link only inside authored window
# ---------------------------------------------------------------------------
replace_once(
    "src/game/tps-game.ts",
    '''import {\n  chooseTpsComboRoute,\n  tpsComboMoveForRoute,\n  type TpsComboRoute,\n} from "./motion-profile";''',
    '''import {\n  chooseTpsComboContinuationRoute,\n  chooseTpsComboRoute,\n  tpsComboLinkWindow,\n  tpsComboMoveForRoute,\n  type TpsComboRoute,\n} from "./motion-profile";''',
    "TPS combo imports",
)
replace_once(
    "src/game/tps-game.ts",
    '''  __comboRoute?: TpsComboRoute;\n  __comboRouteSeed?: number;\n};''',
    '''  __comboRoute?: TpsComboRoute;\n  __comboRouteSeed?: number;\n  __comboLinkSerial?: number;\n  __comboQueuedBranch?: "FORWARD" | "BACK" | "SIDE" | "NEUTRAL";\n};''',
    "TPS combo runtime fields",
)

old_combo = '''  // Confirmed hits can cancel most of recovery after a tiny authored beat. This\n  // turns the old "tap, wait, tap" rhythm into an arcade-style rush while still\n  // refusing to chain whiffs. The next strike stays on the route chosen at the\n  // opening hit, so the three body motions read as one intentional combination.\n  if (\n    game.p1.state === "ATTACK"\n    && activeMove\n    && game.p1.hitTargets.has(game.p2.id)\n    && game.playerComboStage < 3\n  ) {\n    if (rawAttackPressed) game.playerAttackQueued = true;\n    const cancelTick = activeMove.startup + activeMove.active + TPS_HYPE_PROFILE.hitConfirmCancelLagTicks;\n    if (game.playerAttackQueued && game.p1.moveTick >= cancelTick) {\n      game.p1.setInput(input);\n      game.p1.currentMove = null;\n      game.p1.moveTick = 0;\n      game.p1.state = "IDLE";\n      game.p1.hitTargets.clear();\n      game.playerAttackQueued = false;\n      if (prototype.beginContextAttack.call(this)) {\n        game.p1.updatePhysics(FIXED_STEP);\n        hype(game).comboShift(game.playerComboStage);\n        game.audio.comboShift(game.playerComboStage);\n      }\n      return;\n    }\n  }\n'''
new_combo = '''  // Capture direction together with the ATTACK tap. Early taps are allowed to\n  // buffer, but the continuation only fires inside the move's authored\n  // cancelWindow. This preserves a visible follow-through beat before the next\n  // startup and makes direction-based branches deterministic even if the player\n  // releases the stick before the link point arrives.\n  if (game.p1.state === "ATTACK" && rawAttackPressed && game.playerComboStage < 3) {\n    game.__comboQueuedBranch = input.up\n      ? "FORWARD"\n      : input.down\n        ? "BACK"\n        : input.left || input.right\n          ? "SIDE"\n          : "NEUTRAL";\n  }\n\n  if (\n    game.p1.state === "ATTACK"\n    && activeMove\n    && game.p1.hitTargets.has(game.p2.id)\n    && game.playerComboStage < 3\n  ) {\n    const linkWindow = tpsComboLinkWindow(activeMove);\n    if (rawAttackPressed && game.p1.moveTick >= linkWindow.queueStart && game.p1.moveTick <= linkWindow.linkEnd) {\n      game.playerAttackQueued = true;\n    }\n\n    const inLinkWindow = game.p1.moveTick >= linkWindow.linkStart && game.p1.moveTick <= linkWindow.linkEnd;\n    if (game.playerAttackQueued && inLinkWindow) {\n      const linkTick = game.p1.moveTick;\n      const distance = Math.hypot(\n        game.p2.position.x - game.p1.position.x,\n        game.p2.position.z - game.p1.position.z,\n      );\n      const routeFrom = game.__comboRoute ?? chooseTpsComboRoute({\n        distance,\n        flank: false,\n        perfect: false,\n        variationSeed: game.__comboRouteSeed ?? 0,\n      });\n      const branch = game.__comboQueuedBranch ?? "NEUTRAL";\n      const routeTo = chooseTpsComboContinuationRoute({\n        currentRoute: routeFrom,\n        distance,\n        forward: branch === "FORWARD",\n        back: branch === "BACK",\n        side: branch === "SIDE",\n      });\n      game.__comboRoute = routeTo;\n      game.__comboLinkSerial = (game.__comboLinkSerial ?? 0) + 1;\n      game.p1.visual.root.userData.tpsComboLinkSerial = game.__comboLinkSerial;\n      game.p1.visual.root.userData.tpsComboLinkState = "LINKED";\n      game.p1.visual.root.userData.tpsComboLinkFromMove = activeMove.id;\n      game.p1.visual.root.userData.tpsComboLinkTick = linkTick;\n      game.p1.visual.root.userData.tpsComboLinkStart = linkWindow.linkStart;\n      game.p1.visual.root.userData.tpsComboLinkEnd = linkWindow.linkEnd;\n      game.p1.visual.root.userData.tpsComboLinkRouteFrom = routeFrom;\n      game.p1.visual.root.userData.tpsComboLinkRouteTo = routeTo;\n      game.p1.visual.root.userData.tpsComboLinkBranch = branch;\n      game.p1.visual.root.userData.tpsComboLinkBlendSeconds = 0.075;\n\n      game.p1.setInput(input);\n      game.p1.currentMove = null;\n      game.p1.moveTick = 0;\n      game.p1.state = "IDLE";\n      game.p1.hitTargets.clear();\n      game.playerAttackQueued = false;\n      game.__comboQueuedBranch = undefined;\n      if (prototype.beginContextAttack.call(this)) {\n        game.p1.updatePhysics(FIXED_STEP);\n        hype(game).comboShift(game.playerComboStage);\n        game.audio.comboShift(game.playerComboStage);\n      }\n      return;\n    }\n\n    if (game.p1.moveTick > linkWindow.linkEnd) {\n      game.playerAttackQueued = false;\n      game.__comboQueuedBranch = undefined;\n      game.p1.visual.root.userData.tpsComboLinkState = "MISSED";\n    } else if (game.playerAttackQueued) {\n      game.p1.visual.root.userData.tpsComboLinkState = "BUFFERED";\n      game.p1.visual.root.userData.tpsComboLinkStart = linkWindow.linkStart;\n      game.p1.visual.root.userData.tpsComboLinkEnd = linkWindow.linkEnd;\n    }\n  }\n'''
replace_once("src/game/tps-game.ts", old_combo, new_combo, "TPS authored combo link block")

replace_once(
    "src/game/tps-game.ts",
    '''  game.__comboRoute = undefined;\n  game.__comboRouteSeed = 0;\n  game.p1.visual.root.userData.tpsComboRoute = null;''',
    '''  game.__comboRoute = undefined;\n  game.__comboRouteSeed = 0;\n  game.__comboLinkSerial = 0;\n  game.__comboQueuedBranch = undefined;\n  game.p1.visual.root.userData.tpsComboRoute = null;\n  game.p1.visual.root.userData.tpsComboLinkSerial = 0;\n  game.p1.visual.root.userData.tpsComboLinkState = null;\n  game.p1.visual.root.userData.tpsComboLinkFromMove = null;\n  game.p1.visual.root.userData.tpsComboLinkRouteFrom = null;\n  game.p1.visual.root.userData.tpsComboLinkRouteTo = null;\n  game.p1.visual.root.userData.tpsComboLinkBranch = null;''',
    "TPS combo link reset",
)

# ---------------------------------------------------------------------------
# motion-expansion-runtime.ts: presentation-only attack/reaction afterfeel
# ---------------------------------------------------------------------------
replace_once(
    "src/game/motion-expansion-runtime.ts",
    '''type MotionPhase = "STARTUP" | "ACTIVE" | "RECOVERY" | "REACTION" | "AIR" | "DOWN" | "EVASION";\ntype ExpansionRuntime = {''',
    '''type MotionPhase = "STARTUP" | "ACTIVE" | "RECOVERY" | "REACTION" | "AIR" | "DOWN" | "EVASION" | "SETTLE";\ntype MotionTail = {\n  kind: "ATTACK" | "REACTION" | "BLOCK";\n  holdUntil: number;\n  endAt: number;\n  blending: boolean;\n};\ntype ExpansionRuntime = {''',
    "motion tail types",
)
replace_once(
    "src/game/motion-expansion-runtime.ts",
    '''  lastReactionSerial: number;\n  ready: boolean;\n  loading: boolean;\n};''',
    '''  lastReactionSerial: number;\n  lastGameplayState: FighterRuntime["state"];\n  lastAttackPower: number;\n  lastReactionTier: 1 | 2 | 3;\n  lastComboLinkSerial: number;\n  tail: MotionTail | null;\n  ready: boolean;\n  loading: boolean;\n};''',
    "motion runtime tail fields",
)
replace_once(
    "src/game/motion-expansion-runtime.ts",
    '''    lastReactionSerial: -1,\n    ready: false,\n    loading: true,''',
    '''    lastReactionSerial: -1,\n    lastGameplayState: fighter.state,\n    lastAttackPower: 1,\n    lastReactionTier: 1,\n    lastComboLinkSerial: 0,\n    tail: null,\n    ready: false,\n    loading: true,''',
    "motion runtime tail init",
)

old_play = '''function playClip(runtime: ExpansionRuntime, requested: string, loop: boolean, speed: number, restart: boolean): void {\n  const name = resolveClip(runtime, requested);\n  if (runtime.currentClip === name && !restart) return;\n  const clip = runtime.clips.get(name);\n  if (!clip) return;\n  const blend = name.startsWith("Hit_") || name === "Death01" || name === "Idle_Shield_Break" ? 0.025 : 0.045;\n  runtime.currentAction?.fadeOut(blend);\n  const action = runtime.mixer.clipAction(clip, runtime.model);\n  action.reset();\n  action.enabled = true;\n  action.setLoop(loop ? THREE.LoopRepeat : THREE.LoopOnce, loop ? Infinity : 1);\n  action.clampWhenFinished = !loop;\n  action.timeScale = loop ? speed : Math.max(0.25, clip.duration * speed);\n  action.fadeIn(blend).play();\n  runtime.currentClip = name;\n  runtime.currentAction = action;\n}\n'''
new_play = '''function playClip(\n  runtime: ExpansionRuntime,\n  requested: string,\n  loop: boolean,\n  speed: number,\n  restart: boolean,\n  blendOverride?: number,\n): void {\n  const name = resolveClip(runtime, requested);\n  if (runtime.currentClip === name && !restart) return;\n  const clip = runtime.clips.get(name);\n  if (!clip) return;\n  const defaultBlend = name.startsWith("Hit_") || name === "Death01" || name === "Idle_Shield_Break" ? 0.025 : 0.045;\n  const blend = blendOverride ?? defaultBlend;\n  runtime.currentAction?.fadeOut(blend);\n  const action = runtime.mixer.clipAction(clip, runtime.model);\n  action.reset();\n  action.enabled = true;\n  action.setLoop(loop ? THREE.LoopRepeat : THREE.LoopOnce, loop ? Infinity : 1);\n  action.clampWhenFinished = !loop;\n  action.timeScale = loop ? speed : Math.max(0.25, clip.duration * speed);\n  action.fadeIn(blend).play();\n  runtime.currentClip = name;\n  runtime.currentAction = action;\n}\n'''
replace_once("src/game/motion-expansion-runtime.ts", old_play, new_play, "motion clip custom blend")

anchor = '''/**\n * Returns true when the expanded runtime owns the current pose. Neutral/guard\n * states intentionally return false so the older audited UBC ready-pose layer\n * remains authoritative there. The runtime is still primed during neutral so\n * the FIRST attack no longer falls back while the animation GLBs are loading.\n */\n'''
helpers = '''const TAIL_NEUTRAL_STATES = new Set<FighterRuntime["state"]>(["IDLE", "WALK", "CROUCH"]);\nconst COMBO_LINK_BLEND_SECONDS = 0.075;\n\nfunction attackTailTiming(power: number): { hold: number; blend: number } {\n  if (power >= 1.55) return { hold: 0.075, blend: 0.135 };\n  if (power >= 1.05) return { hold: 0.055, blend: 0.115 };\n  return { hold: 0.038, blend: 0.090 };\n}\n\nfunction reactionTailTiming(tier: 1 | 2 | 3, blocked: boolean): { hold: number; blend: number } {\n  if (blocked) return { hold: 0.040, blend: 0.095 };\n  if (tier === 3) return { hold: 0.095, blend: 0.175 };\n  if (tier === 2) return { hold: 0.070, blend: 0.145 };\n  return { hold: 0.050, blend: 0.115 };\n}\n\nfunction beginTail(\n  runtime: ExpansionRuntime,\n  kind: MotionTail["kind"],\n  timeSeconds: number,\n  timing: { hold: number; blend: number },\n): void {\n  runtime.tail = {\n    kind,\n    holdUntil: timeSeconds + timing.hold,\n    endAt: timeSeconds + timing.hold + timing.blend,\n    blending: false,\n  };\n}\n\n'''
replace_once("src/game/motion-expansion-runtime.ts", anchor, helpers + anchor, "motion tail helpers")

old_update = '''export function updateMotionExpansionSkin(fighter: FighterRuntime, opponent: FighterRuntime, timeSeconds: number): boolean {\n  motionReactionFor(fighter);\n  const runtime = ensureRuntime(fighter);\n  if (!EXPANDED_STATES.has(fighter.state)) return false;\n  if (!runtime?.ready) return false;\n\n  const reaction = motionReactionFor(fighter);\n  const desired = desiredMotion(fighter);\n  const restartedMove = fighter.state === "ATTACK" && fighter.moveTick < runtime.lastMoveTick;\n  const restartedReaction = reaction.serial !== runtime.lastReactionSerial && fighter.state !== "ATTACK";\n  runtime.lastMoveTick = fighter.moveTick;\n  runtime.lastReactionSerial = reaction.serial;\n  runtime.currentPhase = desired.phase;\n  playClip(runtime, desired.name, desired.loop, desired.speed, restartedMove || restartedReaction);\n  const delta = runtime.lastTime > 0 ? THREE.MathUtils.clamp(timeSeconds - runtime.lastTime, 0, 0.05) : 0;\n  runtime.lastTime = timeSeconds;\n  runtime.mixer.update(delta);\n  runtime.model.updateMatrixWorld(true);\n  attackSilhouette(runtime, fighter);\n  reactionAccent(runtime, fighter);\n  airborneAccent(runtime, fighter);\n  runtime.model.updateMatrixWorld(true);\n  strikeTrajectory(runtime, fighter, opponent);\n  runtime.model.updateMatrixWorld(true);\n\n  fighter.visual.root.userData.motionExpansionCurrentClip = runtime.currentClip;\n  fighter.visual.root.userData.motionExpansionCurrentMove = fighter.currentMove?.id ?? null;\n  fighter.visual.root.userData.motionExpansionPhase = runtime.currentPhase;\n  fighter.visual.root.userData.motionExpansionContactMode = "OPPONENT_WEIGHTED_IK";\n  return true;\n}\n'''
new_update = '''export function updateMotionExpansionSkin(fighter: FighterRuntime, opponent: FighterRuntime, timeSeconds: number): boolean {\n  const reaction = motionReactionFor(fighter);\n  const runtime = ensureRuntime(fighter);\n  if (!runtime?.ready) return false;\n\n  const previousState = runtime.lastGameplayState;\n  const tailNeutral = TAIL_NEUTRAL_STATES.has(fighter.state);\n\n  if (fighter.state === "ATTACK" && fighter.currentMove) {\n    runtime.lastAttackPower = fighter.currentMove.power;\n    runtime.tail = null;\n  } else if (fighter.state === "HIT" || fighter.state === "BLOCK_STUN") {\n    runtime.lastReactionTier = reaction.tier;\n    runtime.tail = null;\n  } else if (tailNeutral && !runtime.tail) {\n    if (previousState === "ATTACK") {\n      beginTail(runtime, "ATTACK", timeSeconds, attackTailTiming(runtime.lastAttackPower));\n    } else if (previousState === "HIT" || previousState === "BLOCK_STUN") {\n      beginTail(\n        runtime,\n        previousState === "BLOCK_STUN" ? "BLOCK" : "REACTION",\n        timeSeconds,\n        reactionTailTiming(runtime.lastReactionTier, previousState === "BLOCK_STUN"),\n      );\n    }\n  } else if (!tailNeutral && !EXPANDED_STATES.has(fighter.state)) {\n    runtime.tail = null;\n  }\n  runtime.lastGameplayState = fighter.state;\n\n  const delta = runtime.lastTime > 0 ? THREE.MathUtils.clamp(timeSeconds - runtime.lastTime, 0, 0.05) : 0;\n  runtime.lastTime = timeSeconds;\n\n  // Presentation-only settle: gameplay is already free to move/act, but the\n  // rendered body is allowed to finish and briefly hold the final strike/recoil\n  // pose before a slower return to ready stance. Any new combat state cancels\n  // this tail immediately, so responsiveness is unchanged.\n  if (runtime.tail && tailNeutral) {\n    const tail = runtime.tail;\n    if (timeSeconds >= tail.endAt) {\n      runtime.tail = null;\n      runtime.currentPhase = "SETTLE";\n      playClip(runtime, "Idle_Loop", true, 1, false, 0.070);\n      runtime.mixer.update(delta);\n      runtime.model.updateMatrixWorld(true);\n      fighter.visual.root.userData.motionExpansionTailKind = null;\n      fighter.visual.root.userData.motionExpansionTailRemaining = 0;\n      return true;\n    }\n    if (timeSeconds >= tail.holdUntil && !tail.blending) {\n      tail.blending = true;\n      playClip(runtime, "Idle_Loop", true, 1, false, Math.max(0.080, tail.endAt - tail.holdUntil));\n    }\n    runtime.currentPhase = "SETTLE";\n    runtime.mixer.update(delta);\n    runtime.model.updateMatrixWorld(true);\n    fighter.visual.root.userData.motionExpansionCurrentClip = runtime.currentClip;\n    fighter.visual.root.userData.motionExpansionCurrentMove = null;\n    fighter.visual.root.userData.motionExpansionPhase = runtime.currentPhase;\n    fighter.visual.root.userData.motionExpansionTailKind = tail.kind;\n    fighter.visual.root.userData.motionExpansionTailRemaining = Math.max(0, tail.endAt - timeSeconds);\n    fighter.visual.root.userData.motionExpansionContactMode = "OPPONENT_WEIGHTED_IK";\n    return true;\n  }\n\n  if (!EXPANDED_STATES.has(fighter.state)) return false;\n\n  const desired = desiredMotion(fighter);\n  const restartedMove = fighter.state === "ATTACK" && fighter.moveTick < runtime.lastMoveTick;\n  const restartedReaction = reaction.serial !== runtime.lastReactionSerial && fighter.state !== "ATTACK";\n  const comboLinkSerial = Number(fighter.visual.root.userData.tpsComboLinkSerial ?? 0);\n  const comboLinked = restartedMove && comboLinkSerial > runtime.lastComboLinkSerial;\n  if (comboLinkSerial > runtime.lastComboLinkSerial) runtime.lastComboLinkSerial = comboLinkSerial;\n  runtime.lastMoveTick = fighter.moveTick;\n  runtime.lastReactionSerial = reaction.serial;\n  runtime.currentPhase = desired.phase;\n  playClip(\n    runtime,\n    desired.name,\n    desired.loop,\n    desired.speed,\n    restartedMove || restartedReaction,\n    comboLinked ? COMBO_LINK_BLEND_SECONDS : undefined,\n  );\n  runtime.mixer.update(delta);\n  runtime.model.updateMatrixWorld(true);\n  attackSilhouette(runtime, fighter);\n  reactionAccent(runtime, fighter);\n  airborneAccent(runtime, fighter);\n  runtime.model.updateMatrixWorld(true);\n  strikeTrajectory(runtime, fighter, opponent);\n  runtime.model.updateMatrixWorld(true);\n\n  fighter.visual.root.userData.motionExpansionCurrentClip = runtime.currentClip;\n  fighter.visual.root.userData.motionExpansionCurrentMove = fighter.currentMove?.id ?? null;\n  fighter.visual.root.userData.motionExpansionPhase = runtime.currentPhase;\n  fighter.visual.root.userData.motionExpansionTailKind = null;\n  fighter.visual.root.userData.motionExpansionTailRemaining = 0;\n  fighter.visual.root.userData.motionExpansionComboBlendSeconds = comboLinked ? COMBO_LINK_BLEND_SECONDS : 0;\n  fighter.visual.root.userData.motionExpansionContactMode = "OPPONENT_WEIGHTED_IK";\n  return true;\n}\n'''
replace_once("src/game/motion-expansion-runtime.ts", old_update, new_update, "motion afterfeel runtime")

# ---------------------------------------------------------------------------
# tests: lock in authored window, branching, settle tails and combo crossfade
# ---------------------------------------------------------------------------
replace_once(
    "tests/motion-expansion.test.ts",
    '''  chooseTpsComboRoute,\n  motionClipForMove,''',
    '''  chooseTpsComboContinuationRoute,\n  chooseTpsComboRoute,\n  motionClipForMove,''',
    "motion test continuation import",
)
replace_once(
    "tests/motion-expansion.test.ts",
    '''  reactionKindForMove,\n  tpsComboMoveForRoute,''',
    '''  reactionKindForMove,\n  tpsComboLinkWindow,\n  tpsComboMoveForRoute,''',
    "motion test link import",
)
replace_once(
    "tests/motion-expansion.test.ts",
    '''  assert.match(source, /const runtime = ensureRuntime\\(fighter\\);\\s*if \\(!EXPANDED_STATES\\.has\\(fighter\\.state\\)\\) return false;/);''',
    '''  assert.match(source, /const runtime = ensureRuntime\\(fighter\\);/);\n  assert.match(source, /TAIL_NEUTRAL_STATES/);\n  assert.match(source, /motionExpansionTailKind/);\n  assert.match(source, /COMBO_LINK_BLEND_SECONDS = 0\\.075/);\n  assert.match(source, /currentPhase = "SETTLE"/);''',
    "motion runtime source assertions",
)

combo_test_anchor = '''test("left/right contact assignments make adjacent punches visibly alternate limbs", () => {'''
combo_tests = '''test("TPS authored combo links wait for recovery settle and branch inside cancelWindow", () => {\n  const kairo = FIGHTER_DEFINITIONS.red;\n  const jab = kairo.moves.jab;\n  const power = kairo.moves.power;\n  const jabWindow = tpsComboLinkWindow(jab);\n  const powerWindow = tpsComboLinkWindow(power);\n\n  assert.ok(jabWindow.queueStart < jabWindow.linkStart);\n  assert.ok(jabWindow.linkStart > jab.startup + jab.active);\n  assert.ok(jabWindow.linkEnd < jab.startup + jab.active + jab.recovery);\n  assert.ok(jabWindow.linkEnd - jabWindow.linkStart + 1 <= jab.cancelWindow);\n  assert.ok(powerWindow.linkStart > power.startup + power.active);\n\n  assert.equal(chooseTpsComboContinuationRoute({\n    currentRoute: "CLOSE_A", distance: 1.2, forward: false, back: false, side: true,\n  }), "CLOSE_B");\n  assert.equal(chooseTpsComboContinuationRoute({\n    currentRoute: "CLOSE_B", distance: 1.2, forward: true, back: false, side: false,\n  }), "CLOSE_A");\n  assert.equal(chooseTpsComboContinuationRoute({\n    currentRoute: "CLOSE_A", distance: 2.0, forward: true, back: false, side: false,\n  }), "FAR");\n  assert.equal(chooseTpsComboContinuationRoute({\n    currentRoute: "PERFECT", distance: 2.0, forward: false, back: true, side: false,\n  }), "PERFECT");\n});\n\n'''
replace_once("tests/motion-expansion.test.ts", combo_test_anchor, combo_tests + combo_test_anchor, "combo window tests")

replace_once(
    "tests/tps-mode.test.ts",
    '''  assert.match(source, /playerAttackQueued/);\n  assert.match(source, /const comboConfirmed = this\\.p1\\.hitTargets\\.has\\(this\\.p2\\.id\\)/);''',
    '''  assert.match(source, /playerAttackQueued/);\n  assert.match(source, /tpsComboLinkWindow\\(activeMove\\)/);\n  assert.match(source, /chooseTpsComboContinuationRoute/);\n  assert.match(source, /tpsComboLinkState = "BUFFERED"/);\n  assert.match(source, /tpsComboLinkState = "LINKED"/);\n  assert.match(source, /tpsComboLinkBlendSeconds = 0\\.075/);\n  assert.match(source, /__comboQueuedBranch/);\n  assert.match(source, /const comboConfirmed = this\\.p1\\.hitTargets\\.has\\(this\\.p2\\.id\\)/);''',
    "TPS combo link assertions",
)

print("motion afterfeel + authored combo link pass applied")
